# Project Manthan — Environment Template
#
# Set these in your local .env.local or in Vercel Project → Settings → Environment Variables,
# then redeploy. Do not commit secrets.

# Server-only (DO NOT prefix with NEXT_PUBLIC)
ANTHROPIC_API_KEY=sk-...
SUPABASE_SERVICE_ROLE_KEY=service_role_...
FILE_MAX_SIZE_MB=25
PIPELINE_MAX_TOKENS=120000

# Public (exposed to client)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_APP_URL=http://localhost:3000

